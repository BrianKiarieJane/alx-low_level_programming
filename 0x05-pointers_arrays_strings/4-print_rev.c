#include "main.h"
/**
 * print_rev - reverse
 * @s: str
 * Return: 0
 */
void print_rev(char *s)
{
	char *x;
	int k;
	int z;

	k = 0;

	while (s[k] != '\0')
	{
		k++;
		*x = *s;
		for (; *x = *s;)
		{
			*x = z;
			while (*x[k] = '\0')
			{
				_putchar(z);
				k++;
			}
		}
	}
	_putchar('\n');
}
