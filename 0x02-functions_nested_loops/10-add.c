#include "main.h"

/**
 * add- adds two integers and returns the result
 * @x: int 1
 * @y: int 2
 * Return: Results
 */
int add(int x, int y)
{
	int sum;

	sum = add(x, y);
	_putchar(sum);
	return (sum);
}
